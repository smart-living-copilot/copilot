"""Runtime worker entrypoints.

This package groups background and stream processors that run inside the wotbot
runtime (job workers, live voice workers, indexers, and related process-level
helpers).
"""
